server {
<<<<<<< HEAD
    listen 80;
    server_tokens off;
    
    # Use /dev/shm for nginx temp directories to avoid permission issues in Fargate
    client_body_temp_path /dev/shm/nginx/client-body;
    proxy_temp_path /dev/shm/nginx/proxy;
    fastcgi_temp_path /dev/shm/nginx/fastcgi;
    uwsgi_temp_path /dev/shm/nginx/uwsgi;
    scgi_temp_path /dev/shm/nginx/scgi;
    
    # Security headers
    add_header X-Content-Type-Options "nosniff";
    add_header X-XSS-Protection "1; mode=block";
    add_header X-Frame-Options "SAMEORIGIN";
    add_header Content-Security-Policy "default-src 'self'; script-src 'self' 'unsafe-inline'; style-src 'self' 'unsafe-inline'; img-src 'self' data:; font-src 'self'; connect-src 'self' https://*.amazonaws.com";
    add_header Referrer-Policy "strict-origin-when-cross-origin";
    
    # Gzip compression
    gzip on;
    gzip_comp_level 5;
    gzip_min_length 256;
    gzip_proxied any;
    gzip_vary on;
    gzip_types
        application/javascript
        application/json
        application/xml
        text/css
        text/plain
        text/xml;
    
    location / {
        root /usr/share/nginx/html;
        index index.html index.htm;
        try_files $uri $uri/ /index.html;
        
        # Cache static assets
        location ~* \.(js|css|png|jpg|jpeg|gif|ico|svg)$ {
            expires 30d;
            add_header Cache-Control "public, no-transform";
        }
    }
    
    # Serve static assets
    location /assets/ {
        alias /usr/share/nginx/html/assets/;
        expires 30d;
        add_header Cache-Control "public, no-transform";
        try_files $uri $uri/ =404;
    }
    
    # API endpoints - will be routed through ALB
    location /api/ {
        # In production, these will be handled by the ALB routing rules
        proxy_pass http://localhost:8080/api/;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
    
    # Deny access to hidden files
    location ~ /\. {
        deny all;
        access_log off;
        log_not_found off;
=======
    listen 8080;
    server_name _;
    root /usr/share/nginx/html;
    index index.html;

    # Logs to stdout/stderr
    access_log /dev/stdout;
    error_log /dev/stderr;

    # Handle SPA routing
    location / {
        try_files $uri $uri/ /index.html;
    }

    # Serve static assets with cache headers
    location /assets/ {
        expires 7d;
        add_header Cache-Control "public, max-age=604800";
>>>>>>> origin/testing
    }
}